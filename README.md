Since the original project is closed, I´ve updated the current release with the newest Ollama version:
    -Qwen3.5B Models can now be loaded
    -Models >10GB can be loaded (even with shared Memory)
    -iGPU native implementation for faster GPU support.

Only updated for windows devices at the moment.
